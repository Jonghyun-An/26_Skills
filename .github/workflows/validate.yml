name: validate

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  skills:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate packages
        run: python3 scripts/validate_skills.py
      - name: Check installer syntax
        run: bash -n skills/install.sh

